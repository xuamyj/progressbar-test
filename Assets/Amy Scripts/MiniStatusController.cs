using UnityEngine;

public enum TrainingStatus
{
    Collab,
    Perfect,
    Great,
    Good,
    NewSkill,
    Other,
}

public enum CharacterName // move this later
{
    Ava,
    Isadora,
    Malcolm,
    Nadira,
    Evander,
}

public class MiniStatusController : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
